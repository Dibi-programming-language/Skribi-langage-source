name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v5
    - uses: determinate-systems/nix-installer-action@v1
    - uses: determinate-systems/magic-nix-cache-action@v1
    - name: Build
      run: nix develop --command cargo build --verbose
    - name: Run tests
      run: nix develop --command cargo test --verbose
    - name: Run fmt
      run: nix develop --command cargo fmt --all -- --check
